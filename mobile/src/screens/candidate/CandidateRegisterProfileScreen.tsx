import React from 'react';
import {Text, View} from 'react-native';

export const CandidateRegisterProfileScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
      }}>
      <Text>CandidateRegisterProfileScreen</Text>
    </View>
  );
};
